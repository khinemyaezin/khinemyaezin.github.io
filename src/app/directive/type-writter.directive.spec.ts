import { TypeWritterDirective } from './type-writter.directive';

describe('TypeWritterDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeWritterDirective();
    expect(directive).toBeTruthy();
  });
});
